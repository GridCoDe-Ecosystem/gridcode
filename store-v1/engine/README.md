StoreVault Engine — Canonical Implementation

Use the `.js` files in this directory for runtime integration.

The `.ts` files are the original source.
The `.d.ts` and `.map` files are build artifacts.

Gridnet integration should use:
  StoreVault.js
  transitions.js
  guards.js
  receipts.js
  types.js
