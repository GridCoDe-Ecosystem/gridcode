GridCoDe Store v1 — dApp Layer

Entry point:
  CStoreGrid.app

This is the Gridnet-native UI shell.

It imports:
  /store_v1/engine/*
  /store_v1/adapter/*

CStoreGrid.js and rollup.config.mjs are legacy artifacts from a previous bundling approach and are not used in the current deployment model.
